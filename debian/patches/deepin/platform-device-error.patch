commit 4258e361b6a3e9bb8bfb1ae331293ab8ed7ec96f
Author: Yang Bo <yangbo@deepin.com>
Date:   Tue Mar 21 15:03:47 2017 +0800

    platform device error
    
    Signed-off-by: Yang Bo <yangbo@deepin.com>

diff --git a/drivers/acpi/acpi_platform.c b/drivers/acpi/acpi_platform.c
index b4c1a6a51..27e1cd5bf 100644
--- a/drivers/acpi/acpi_platform.c
+++ b/drivers/acpi/acpi_platform.c
@@ -117,7 +117,7 @@ struct platform_device *acpi_create_platform_device(struct acpi_device *adev,
 
 	pdev = platform_device_register_full(&pdevinfo);
 	if (IS_ERR(pdev))
-		dev_err(&adev->dev, "platform device creation failed: %ld\n",
+		dev_notice(&adev->dev, "platform device creation failed: %ld\n",
 			PTR_ERR(pdev));
 	else
 		dev_dbg(&adev->dev, "created platform device %s\n",
diff --git a/drivers/base/platform.c b/drivers/base/platform.c
index c4af00385..c754858ad 100644
--- a/drivers/base/platform.c
+++ b/drivers/base/platform.c
@@ -396,7 +396,7 @@ int platform_device_add(struct platform_device *pdev)
 		}
 
 		if (p && insert_resource(p, r)) {
-			dev_err(&pdev->dev, "failed to claim resource %d\n", i);
+			dev_notice(&pdev->dev, "failed to claim resource %d\n", i);
 			ret = -EBUSY;
 			goto failed;
 		}
