commit deb43cbc66723eecedf3d60e94b6b7f550c7c3d2
Author: Yang Bo <yangbo@deepin.com>
Date:   Mon Sep 26 16:12:41 2016 +0800

    EDID failure in 0x0f31
    
    Signed-off-by: Yang Bo <yangbo@deepin.com>

diff --git a/drivers/gpu/drm/i915/intel_crt.c b/drivers/gpu/drm/i915/intel_crt.c
index 6a2c76e..b317fb5 100644
--- a/drivers/gpu/drm/i915/intel_crt.c
+++ b/drivers/gpu/drm/i915/intel_crt.c
@@ -682,7 +682,7 @@ static int intel_crt_get_modes(struct drm_connector *connector)
 
 	i2c = intel_gmbus_get_adapter(dev_priv, dev_priv->vbt.crt_ddc_pin);
 	ret = intel_crt_ddc_get_modes(connector, i2c);
-	if (ret || !IS_G4X(dev))
+	if (ret || !(IS_G4X(dev) || (dev->pdev->device == 0x0f31)))
 		goto out;
 
 	/* Try to probe digital port for output in DVI-I -> VGA mode. */
